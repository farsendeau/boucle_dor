{% extends 'base.html.twig' %}

{% block title %}Email de réinitialisation envoyé{% endblock %}

{% block body %}
<div class="container d-flex align-items-center justify-content-center" style="min-height: calc(100vh - 200px);">
    <div class="card shadow rounded-0 p-4 col-md-6 col-lg-4">
    <div class="alert alert-info">
        <p>Si un compte est associé à cette adresse, un email lui sera envoyé afin de réinitialier le mdp</p>
        <p class="mb-0">Lien valide pendant {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>
    </div>

    <a href="{{ ea_url().setController('App\\Controller\\Admin\\UserCrudController') }}"
       class="btn btn-primary">
        <i class="fas fa-arrow-left me-2"></i>Retour aux utilisateurs
    </a>
    </div>
 </div>
{% endblock %}

<!DOCTYPE html>
<html lang="{{ app.locale|default('fr') }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ 'error_page.error'|trans([], null, app.locale)}} {{ status_code }}</title>
    <!-- Bootstrap CSS si vous l'utilisez -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Vos styles personnalisés -->
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f8f9fa;
            margin: 0;
            padding: 0;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }
        .error-container {
            text-align: center;
            padding: 2rem;
            background-color: white;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            max-width: 500px;
            width: 90%;
        }
        h3 {
            margin-bottom: 1rem;
            color: #dc3545;
        }
        p {
            margin-bottom: 2rem;
            color: #6c757d;
        }
        .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
            color: white;
            text-decoration: none;
            padding: 0.5rem 1rem;
            border-radius: 4px;
            font-weight: bold;
            display: inline-block;
            transition: background-color 0.2s;
        }
        .btn-primary:hover {
            background-color: #0069d9;
            border-color: #0062cc;
        }
    </style>
</head>
<body>
<div class="error-container">
    <h3>{{ 'error_page.error_title'|trans }}</h3>
    <p>{{ 'error_page.error_p'|trans }}</p>
    <a href="{{ path('home_index') }}" class="btn btn-primary">{{ 'home.home'|trans([], null, app.locale)|default('Retour à l\'accueil') }}</a>
</div>
</body>
</html>
